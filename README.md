# python-flask
Learn python flask

## Run the sample application

From the project root, run:

```bash
python myApp/app.py
```

Then open <http://127.0.0.1:5000/info> in a browser. In PyCharm, right-click
`myApp/app.py` and select **Run 'app'**, then visit the same URL.
